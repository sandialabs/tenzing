add_executable(spmv spmv.cu)
target_link_libraries(spmv sched)
set_standards(spmv)
set_options(spmv)
target_include_directories(spmv SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-comparison spmv_comparison.cu)
target_link_libraries(spmv-comparison sched)
set_standards(spmv-comparison)
set_options(spmv-comparison)
target_include_directories(spmv-comparison SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-stream-brute spmv_stream_brute.cu)
target_link_libraries(spmv-stream-brute sched)
set_standards(spmv-stream-brute)
set_options(spmv-stream-brute)
target_include_directories(spmv-stream-brute SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-stream-brute2 spmv_stream_brute2.cu)
target_link_libraries(spmv-stream-brute2 sched)
set_standards(spmv-stream-brute2)
set_options(spmv-stream-brute2)
target_include_directories(spmv-stream-brute2 SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-mcts-balancehist spmv_mcts_balancehist.cu)
target_link_libraries(spmv-mcts-balancehist sched)
set_standards(spmv-mcts-balancehist)
set_options(spmv-mcts-balancehist)
target_include_directories(spmv-mcts-balancehist SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-mcts-coverage spmv_mcts_coverage.cu)
target_link_libraries(spmv-mcts-coverage sched)
set_standards(spmv-mcts-coverage)
set_options(spmv-mcts-coverage)
target_include_directories(spmv-mcts-coverage SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-mcts-normanticorr spmv_mcts_normanticorr.cu)
target_link_libraries(spmv-mcts-normanticorr sched)
set_standards(spmv-mcts-normanticorr)
set_options(spmv-mcts-normanticorr)
target_include_directories(spmv-mcts-normanticorr SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)


add_executable(spmv-mcts-normrootcorr spmv_mcts_normrootcorr.cu)
target_link_libraries(spmv-mcts-normrootcorr sched)
set_standards(spmv-mcts-normrootcorr)
set_options(spmv-mcts-normrootcorr)
target_include_directories(spmv-mcts-normrootcorr SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-mcts-random spmv_mcts_random.cu)
target_link_libraries(spmv-mcts-random sched)
set_standards(spmv-mcts-random)
set_options(spmv-mcts-random)
target_include_directories(spmv-mcts-random SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)

add_executable(spmv-mcts-unvisited spmv_mcts_unvisited.cu)
target_link_libraries(spmv-mcts-unvisited sched)
set_standards(spmv-mcts-unvisited)
set_options(spmv-mcts-unvisited)
target_include_directories(spmv-mcts-unvisited SYSTEM PRIVATE ${CMAKE_SOURCE_DIR}/thirdparty)